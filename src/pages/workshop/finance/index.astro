---
import Breadcrumbs from '@/components/shared/breadcrumbs.astro'
import PageHead from '@/components/shared/page-head.astro'
import Layout from '@/layouts/layout.astro'
import { getEntry } from 'astro:content'
import Finance from '../../../components/tools/finance'

const tool = await getEntry('workshop', 'finance')
if (!tool) throw new Error('Workshop introuvable')
const { name, description, icon } = tool.data
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title={name} description={description} />

  <Breadcrumbs
    items={[
      { label: 'Workshop', href: '/workshop', icon: 'lucide:calendar' },
      { label: name, icon: icon ?? 'lucide:wrench' },
    ]}
  />

  <Finance client:load />
</Layout>
