---
import Breadcrumbs from '@/components/shared/breadcrumbs.astro'
import PageHead from '@/components/shared/page-head.astro'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import Layout from '@/layouts/layout.astro'
import { Icon } from 'astro-icon/components'
import { getEntry } from 'astro:content'

const tool = await getEntry('tools', 'finance')
if (!tool) throw new Error('Outil introuvable')
const { name, description, icon } = tool.data
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title={name} description={description} />

  <Breadcrumbs
    items={[
      { label: 'Outils', href: '/tools', icon: 'lucide:wrench' },
      { label: name, icon: icon ?? 'lucide:tool' },
    ]}
  />

  <div class="flex flex-col gap-6">
    <Card>
      <CardHeader>
        <CardTitle>{name}</CardTitle>
      </CardHeader>

      <CardContent>
        <div class="bg-muted/20 space-y-4 rounded-md p-3">
          <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
            <div class="space-y-2">
              <label
                for="income-input"
                class="text-muted-foreground text-xs font-medium"
              >
                Revenus totaux
              </label>
              <input
                id="income-input"
                type="number"
                min="0"
                step="0.01"
                placeholder="2500"
                class="border-border bg-background w-full rounded border p-2 text-sm"
              />
            </div>

            <div class="space-y-2">
              <label
                for="fixed-input"
                class="text-muted-foreground text-xs font-medium"
              >
                Charges fixes
              </label>
              <input
                id="fixed-input"
                type="number"
                min="0"
                step="0.01"
                placeholder="1200"
                class="border-border bg-background w-full rounded border p-2 text-sm"
              />
            </div>
          </div>
        </div>

        <div
          class="border-border/50 mt-4 grid grid-cols-1 gap-4 border-t pt-4 md:grid-cols-4"
        >
          <div>
            <p class="text-muted-foreground text-xs font-medium">
              Reste disponible
            </p>
            <p id="free-output" class="mt-1 font-mono text-lg font-semibold">
              0,00 €
            </p>
          </div>

          <div>
            <p class="text-muted-foreground text-xs font-medium">% alloué</p>
            <p
              id="allocated-percent-output"
              class="mt-1 font-mono text-lg font-semibold"
            >
              0 %
            </p>
          </div>

          <div>
            <p class="text-muted-foreground text-xs font-medium">% restant</p>
            <p
              id="remaining-percent-output"
              class="mt-1 font-mono text-lg font-semibold"
            >
              100 %
            </p>
          </div>

          <div>
            <p class="text-muted-foreground text-xs font-medium">
              Montant restant
            </p>
            <p
              id="remaining-amount-output"
              class="mt-1 font-mono text-lg font-semibold"
            >
              0,00 €
            </p>
          </div>
        </div>

        <div class="bg-muted mt-4 h-2 overflow-hidden rounded-full">
          <div
            id="allocation-progress"
            class="bg-primary h-full w-0 transition-all duration-200"
          >
          </div>
        </div>

        <p
          id="allocation-warning"
          class="text-destructive mt-3 hidden text-sm font-medium"
        >
          Le total des enveloppes dépasse 100 % du reste disponible.
        </p>
      </CardContent>
    </Card>

    <Card>
      <CardHeader>
        <div
          class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"
        >
          <CardTitle>Enveloppes</CardTitle>

          <div class="flex flex-wrap gap-2">
            <button
              id="preset-envelopes"
              class="border-border bg-background hover:bg-muted rounded border px-3 py-1.5 text-sm transition-colors"
              type="button"
            >
              <span class="flex items-center gap-1.5">
                <Icon name="lucide:sparkles" class="size-3.5" />
                Préremplir
              </span>
            </button>

            <button
              id="add-envelope"
              class="border-border bg-background hover:bg-muted rounded border px-3 py-1.5 text-sm transition-colors"
              type="button"
            >
              <span class="flex items-center gap-1.5">
                <Icon name="lucide:plus" class="size-3.5" />
                Ajouter une enveloppe
              </span>
            </button>

            <button
              id="reset-form"
              class="border-border bg-background hover:bg-muted rounded border px-3 py-1.5 text-sm transition-colors"
              type="button"
            >
              <span class="flex items-center gap-1.5">
                <Icon name="lucide:rotate-ccw" class="size-3.5" />
                Réinitialiser
              </span>
            </button>
          </div>
        </div>
      </CardHeader>

      <CardContent>
        <div id="envelopes-list" class="flex flex-col gap-3">
          <div
            id="empty-envelopes"
            class="border-border text-muted-foreground rounded-md border border-dashed px-4 py-8 text-center text-sm"
          >
            Aucune enveloppe ajoutée. Clique sur “Ajouter une enveloppe” pour
            commencer.
          </div>
        </div>
      </CardContent>
    </Card>
  </div>

  <script src="./_finance.ts"></script>
</Layout>
